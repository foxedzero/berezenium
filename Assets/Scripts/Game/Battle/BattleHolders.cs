using UnityEngine;

public class BattleHolders : MonoBehaviour
{
    //[SerializeField] private BattlerHolder[] PlayerSide;
    //[SerializeField] private BattlerHolder[] EnemySide;

    //[SerializeField] private BattleProvider BattleProvider;

    //private void Start()
    //{
    //    for(int i = 0; i < BattleProvider._PlayerSide.Length; i++)
    //    {
    //        PlayerSide[i].gameObject.SetActive(true);
    //        PlayerSide[i].SetBattler(BattleProvider._PlayerSide[i]);
    //    }
    //    for (int i = 0; i < BattleProvider._EnemySide.Length; i++)
    //    {
    //        EnemySide[i].gameObject.SetActive(true);
    //        EnemySide[i].SetBattler(BattleProvider._EnemySide[i]);
    //    }
    //}
}
